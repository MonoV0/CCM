"""
チャンネル管理Bot
----------------------------------
・サーバー参加時に、ルール確認→参加区分選択→学年選択のオンボーディングを行い、
  個人チャンネルを自動作成する。
・個人チャンネルの紐付け管理（手動紐付け・一括同期・検索・監査）。
・お気に入りチャンネル機能（本人専用のブックマークカテゴリ）。
・退出時の「削除して退出」「非表示にして退出」選択と、非表示チャンネルの
  自動削除（保持期間経過後）。
・招待申請フォーム→管理者承認フロー。
・データの取り扱いについての説明、本人によるセルフサービス（データ確認／
  エクスポート／削除）。

このファイルはエントリーポイント。実際の処理は役割ごとに分かれた
モジュール（storage / bot_core / utils / views / events / commands）に
置かれている。
"""

from bot_core import bot, TOKEN

# 各モジュールをインポートすることで、中の @bot.tree.command や @bot.event
# デコレータが実行され、コマンド／イベントがbotに登録される。
import views  # noqa: F401
import events  # noqa: F401
import commands  # noqa: F401


if __name__ == "__main__":
    bot.run(TOKEN)
